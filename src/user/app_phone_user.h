#ifndef APP_PHONE_USER_H
#define APP_PHONE_USER_H

#include "../callbacks/app_phone_callbacks.h"
#include "../ui/app_phone_ui.h"

/**
 * 用户自定义头文件
 * 此文件只生成一次，可自由修改
 */

// 在此添加自定义声明
void phone_missed_call_selected_cb(void *obj, gui_event_t *e);

#endif // APP_PHONE_USER_H
